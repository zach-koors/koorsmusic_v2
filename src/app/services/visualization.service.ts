import { Injectable } from '@angular/core';

// Minimal VisualizationService skeleton for Phase 4. Implementation planned in Phase 6.
@Injectable({ providedIn: 'root' })
export class VisualizationService {
  start(canvas: HTMLCanvasElement) {
    // begin render loop (Phase 6)
  }

  stop() {
    // stop render loop
  }
}
